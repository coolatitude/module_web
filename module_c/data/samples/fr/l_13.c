#include "game_functions.h"

/*	La boucle for fait exactement la meme chose que la boucle while
**	mais la syntaxe est legerement differente. Elle permet de declarer la
**	variable incrementee (iterateur), ecrire ta condition et incrementer
**	l'iterateur sur la meme ligne.
**
**	Tu peux l'ecrire comme ceci :
**	for (int iterator = 1 ; condition ; iterator++)
**	{
**		code a executer
**	}
**
**	Note : iterator++ est la facon courte d'ecrire
**		iterator = iterator + 1
**	Ca s'appelle un operateur d'incrementation
**/

void player_function()
{
	for (int iterator = 0 ; iterator <= 20 ; iterator++)
	{
		//En utilisant la boucle for, tu peux economiser 2 lignes. Interessant...
		//Quelque chose manque peut etre au code pour completer le niveau...
	}
}
