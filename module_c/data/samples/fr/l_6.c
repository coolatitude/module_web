#include "game_functions.h"
void player_function()
{
	int direction; //Ici, on declare une variable appellee "direction"

	direction = UP; //ici, on assigne la valeur "UP" a la variable "direction"

	walk(direction); //Ici, on utilise le contenu de "direction" dans la fonction "walk"
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
}
