#include "game_functions.h"
void player_function()
{
	int richting; //Hier declareren we variabele genoemd "richting"

	richting = UP; //Hier geven we de waarde "UP" aan onze variabele "richting"

	walk(richting); //Hier gebruiken we de variabele "richting"
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
	walk(richting);
}
