#include "game_functions.h"
void player_function()
{
	int direction; //Here we DECLARE the variable called "direction"

	direction = UP; //Here we ASSIGN the value "UP" to the variable "direction"
	
	walk(direction); //Here we use the content of "direction" into the "walk" function
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
	walk(direction);
}
